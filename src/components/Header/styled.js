import styled from "styled-components";

export const Logo = styled.img`
  height: 64px;
  margin-right: 16px;
  width: 64px;
`;

export const Title = styled.h1`
  color: #fff;
  font-size: 32px;
  line-height: 38px;
  margin: 0;
`;

export const Wrapper = styled.header`
  background-color: #2bb9c8;
  display: flex;
  padding: 24px;
  position: sticky;
  place-content: center;
  place-items: center;
  top: 0;
  z-index: 1;
`;
